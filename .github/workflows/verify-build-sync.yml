name: Verify architect build sync

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify-build-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check version.json matches HTML meta tags
        run: node scripts/verify-architect-build-sync.mjs
